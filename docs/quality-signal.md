# Quality Signal

This repository is a public-safe showcase, not a production package.

The GitHub Actions workflow checks syntax for public PHP sample files under `samples/`. It does not install WordPress, WooCommerce, Composer dependencies, a database, or private implementation code.

The check exists to keep the public examples clean and reviewable. It does not validate production runtime behavior.
